This error occurs when using the Expo `Linking` API to handle deep links.  The `getInitialURL` async function sometimes returns `null` even when a deep link is successfully opened. This is inconsistent and makes it difficult to reliably handle the initial URL.